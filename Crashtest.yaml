apiVersion: apps/v1
kind: Deployment
metadata:
  name: crash-startup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crash-test
  template:
    metadata:
      labels:
        app: crash-test
    spec:
      containers:
      - name: alpine
        image: alpine
        # Cette commande n'existe pas, le pod va crash immédiatement
        command: ["/bin/sh", "-c", "exit 1"]
